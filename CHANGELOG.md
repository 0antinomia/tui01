# Changelog

## 0.1.0

首个可接入版本，当前范围包括：

- 四分区 TUI 壳层
- 菜单、内容区、日志区等基础组件
- `AppSpec` / `AppConfig` 双入口
- YAML / JSON / Lua 配置加载
- `RuntimeHost` 宿主接入层
- 已注册动作、shell 策略、cwd/env 白名单
- 宿主 logger / event hook
- 宿主模板工程与可运行 example
